/**
 * var let const 
 * default parameter
 * template string
 * arrow function
 * destructuring and separate operator
 */

const a = 56;
const numbers = [21, 52, 50, 45, 41, 40];
const person = {
    name: 'kobir alom',
}

const message = `Hi ${person.name} to get the ${numbers[3]} and come from${a}`
if(true){

}

const square = x => x * x;
const sum = (a, b) => a + b;

const {age, z} = {x: 2, y: 4, z: 9, name: 'zaman khan', age: 55}