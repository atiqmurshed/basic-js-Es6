const numbere = [12, 54, 60, 10, 80, 70];
const total = numbere.reduce((previous, current) => previous + current, 0);
console.log(total);

const sum = numbere.reduce((p, c) => p+c, 0);
console.log(sum);

